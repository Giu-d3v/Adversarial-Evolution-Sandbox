# 《对抗演化 · 涌现》游戏说明

> 基于《对抗演化与合作跃升》一书核心机制做的演化沙盒。  
> 跑：`python game/adversarial_evolution.py`

---

## 一、本游戏和这本书是什么关系

这本书（《对抗演化与合作跃升》）想回答一个根本问题：**从单细胞到人类文明，几亿年的演化靠什么驱动？** 答案是一个贯穿全书的**核心张力**：

> **对抗演化 (Adversarial Evolution)** ↔ **合作跃升 (Cooperative Ascension)**

- **对抗演化**：个体 / 群体之间为资源、配偶、生存展开竞争，"适者生存"的常规理解
- **合作跃升**：通过紧密合作，组织复杂度与能力实现**维度跃升**（单细胞 → 真核 → 多细胞 → 人类 → 文明）

这本书提出两个**量化定律**作为判据：
- **合作红利定律** `1+1>2`：合作产出严格大于个体独立产出之和
- **夺利亏损定律** `1+1<2`：过度争夺导致群体价值净损耗

**本游戏是这两个定律的沙盒演练**：你看到的世界是一片黑暗中的小圆点，颜色编码它们在 (攻击性, 合作性) 二维性状空间的角度。**没有任何预设的策略标签** —— 书中讲的所有演化策略（夺利、互助合作、修身、好善疾恶、伪善、搭便车…）都从 8 个随机性状的组合中**自然涌现**。你按 R 重置一次，演化剧本就重写一次。

---

## 二、书中核心概念 → 游戏设计的映射

下面这张表是**设计母题表**。每行：一个书里反复出现的概念，对应游戏里什么机制，以及当前实现到了哪一步。

### 已实现（核心机制）

| 书中概念 | 书中含义 | 游戏对应 | 当前代码位置 |
|---|---|---|---|
| **夺利主义 Grabbingism** | 主动争夺资源；符合 `1+1<2` | `aggression` 性状高 + 饥饿时主动攻击弱者 | `_agent_act` 攻击分支 |
| **互助合作主义 Mutualistic Cooperation** | 通过协作实现 `1+1>2` | `cooperation` 性状高 + 同 hue 邻居分享 0.06 能量 | `_agent_act` 分享分支 |
| **修身主义 Self-Cultivationism** | 提升自身适应性，不依赖群体 | `aggression≈0`, `cooperation≈0`, `metabolism` 低 | 性状空间自然涌现 |
| **合作红利定律 `1+1>2`** | 合作产出 > 个体之和 | 同色分享 = 双方获能，`hue 距离 < 35°` 触发 | `_agent_act` |
| **夺利亏损定律 `1+1<2`** | 争夺导致净损耗 | 攻击者付"夺利税" `0.06` + 防守者按 `defense` 减伤 | `_agent_act` 攻击分支 |
| **针锋相对 (Tit-for-Tat)** | "你帮我我帮你，你抢我我抢你" | **未实现** ❌（见第三章） | — |
| **短视夺利递进律** | 无约束时夺利不断加码的必然趋势 | **自然涌现** —— 当前无惩罚机制时 `avgAgg` 单调上升 | 实证：seed=42 `avgAgg 0.53 → 0.78` |
| **霍布斯丛林 Hobbesian Jungle** | 所有人对抗所有人 | 失败态 —— 种群红黄为主、avgAgg≈0.75、avgCoop≈0.35 | 实证稳态 |
| **进化稳定策略 ESS** | 不可被入侵的策略组合 | **自然涌现** —— 当前唯一 ESS 是"夺利接管" | 可由 avgAgg / avgCoop 收敛判断 |
| **善选择 Virtue Selection** | 群体筛选/惩罚夺利者 | **未实现** ❌（见第三章） | — |
| **跨维度自相似** | 不同尺度的演化结构相似 | 沙盒本身 —— 微观小球群 = 宏观文明演化 | 游戏整体作为类比 |
| **大氧化事件 Great Oxidation Event** | 环境剧变倒逼重选 | **未实现** ❌（见第三章） | — |
| **维度跃升 / 维度坍缩** | 合作层级跃升 / 跌落 | **未实现** ❌（无显式阶段） | — |
| **逆淘汰 Reverse Selection** | 善者被淘汰、夺利者扩张 | 现象已隐含，但**没有 UI 显式呈现** | — |

### 当前未实现但书中讨论的（差距清单）

| 概念 | 重要性 | 大致工作量 |
|---|---|---|
| **善选择 / 合作维护机制**：可攻击夺利者 + 维护成本 | **最高** —— 缺它就出不来 GVBE / 大跃进期 | 低 |
| **针锋相对 TFT**：每 agent 维护邻居记忆表 | 高 —— 是 GVBE 的前提 | 中 |
| **搭便车主义 Free-Riderism** 检测：接收分享但不分享 | 高 —— 概念上与"伪装合作"紧密相连 | 低（伴随 punish 自然涌现） |
| **逆淘汰可视化**：在 HUD 画"善者存活率"曲线 | 高 —— 把书的 climax 直接呈现 | 极低 |
| **大氧化事件 / 环境剧变**：每 N tick 触发一次环境切换 | 高 —— 让游戏有"阶段感" | 中 |
| **维度跃升 5 阶段**叙事：修 → 朴素合作 → 互惠 → GVBE → 大跃进 | 高 —— 给玩家一个 journey 感 | 低（叙事层） |
| **群体选择**：按空间局部性形成"群" | 中 —— 中卷核心 | 高 |
| **超个体主义**：分化出 caste | 中 | 高 |
| **性择逆向**："渣"基因扩散 | 中 | 中 |
| **信息素 / 路径标记**：化学信号层 | 中 | 中 |
| **量化善恶**：每个 agent 的"善贡献分" | 低 —— UI 增强 | 极低 |
| **内共生事件**：两个 agent 显式融合 | 低 —— 一次性剧情事件 | 低 |
| **伪善主义 (完整版)**：信任建立后背叛 | 低 —— 需要记忆 + 信号 + 欺骗 | 高 |
| **大腐化期 / 伊甸园期**：显式状态命名 | 低 —— 叙事 | 极低 |
| **混沌对抗困境**：长周期资源压力 | 低 —— 终局机制 | 中 |
| **群际好善疾恶**：周礼 / 罗马跨群体合作 | 低 | 极高 |

最值得优先补的两件事：
1. **善选择（punish 动作）** —— 一个动作解锁 GVBE、大跃进、伊甸园、霍布斯丛林、维度坍缩**五个**书中剧情状态
2. **逆淘汰 / 维度跃升可视化** —— 不改机制，只把已经涌现的现象画成曲线，让玩家直观读到书的 climax

---

## 三、怎么玩

### 3.1 旁观模式（默认）

不做任何事，看着种群演化：

1. 按 `R` 几次，换种子看不同剧本
2. 重点看右上角 **aggression / cooperation 直方图** —— 形状变化就是自然选择在工作
3. 看底部 **策略占比** 行 —— 「夺利 / 互助合作 / 修身 / 好善疾恶 / 未分化」五种标签的此消彼长

### 3.2 干扰模式（可选）

世界里的个体都从一个**全随机的初始基因池**开始。如果你往某个区域连续注入 (`N` 模式) 几个全随机新个体，相当于**基因洪流 / 移民**，能重置局部策略分布。`F` 模式投放食物则相当于**人为环境宽松** —— 局部缓解饥荒。

| 按键 | 作用 |
|---|---|
| `SPACE` | 暂停 / 继续 |
| `R` | 用新随机种子重置世界 |
| `N` | 切点击模式 = 注入新个体（点击位置生成全随机性状的新个体） |
| `F` | 切点击模式 = 投放食物（点击位置放一团能量） |
| `+` / `-` | 仿真速度 ±0.5x（上限 8x） |
| `ESC` | 退出 |

---

## 四、屏幕上你看什么

### 4.1 世界里

- **小圆点 = 个体**
  - **颜色 hue**（色相）= 个体在 `(aggression, cooperation)` 二维性状空间的角度。**策略从颜色直接读出来**：
    - 🔴 红（hue ≈ 0°）= 高攻击 / 低合作 → **夺利**
    - 🟢 绿（hue ≈ 120°）= 高合作 / 低攻击 → **互助合作**
    - 🟣 紫（hue ≈ 300°）= 两者都低 → **修身**
    - 🟡 黄（两者都中等）= **未分化** 或 **伪善**（见下方说明）
  - **大小** = 当前能量（energy）—— 越大越接近繁殖
  - **亮度** = 能量饱和度
- **绿色小点 = 食物**（半径越大 = 能量越多）

### 4.2 顶部 HUD

```
tick N    gen M    pop K    food F    births B  deaths D    running x1.0  mode=agent
```

- `tick` — 已运行 tick 数
- `gen` — 当前最长世代（最高 gen 编号）
- `pop` — 当前活体数
- `food` — 当前食物数
- `births` / `deaths` — 累计出生 / 死亡

### 4.3 右上角

- **aggression** / **cooperation** 直方图 —— 种群在两个核心性状上的分布实时变化。这是看 **自然选择怎么动** 的主窗口。
- `frame N  step-err N` —— 每秒应涨 ~60，不动 = 卡死。

### 4.4 策略占比行

按当前 `(aggression, cooperation)` 象限粗略统计的策略分布：

```
策略占比  夺利 142  未分化 38  互助合作 21
```

会随时间漂移 —— 这就是**涌现**。

---

## 五、你应当看到什么 —— 几类典型轨迹 ↔ 书中理论

| 轨迹 | 现象 | 书中对应 | 当前代码会涌现吗 |
|---|---|---|---|
| **夺利接管** | avgAgg 从 0.5 升到 ~0.75，avgCoop 跌到 ~0.35，种群进入红黄为主 | 短视夺利递进律 → 霍布斯丛林 | ✅ 自然涌现（默认轨迹） |
| **合作繁荣-崩溃** | 偶尔一波种群飙到几百、平均变绿，然后食物耗尽大崩盘到几十 | 混沌对抗困境 | ✅ seed=42 在 t≈750 会出现 |
| **伊甸园期** | 罕见窗口：种群稳、avgAgg ≈ avgCoop ≈ 0.5、几乎不死 | 大跃进时期 / 伊甸园时期 | ⚠️ 偶尔出现但不持久，因为**没有善择机制稳住合作** |
| **逆淘汰** | 互助合作者占比从 30% → 10% → 1%，被夺利者覆盖 | 逆淘汰 / 短视夺利递进 | ⚠️ 现象已存在，但**目前没有 HUD 显式画出"善者存活率"** |
| **维度跃升** | 群体从无分享 → 出现小聚簇 → 聚簇变大 | 维度跃升（前提条件） | ⚠️ 现象有，但**当前没有"达到 GVBE 阶段"的状态提示** |
| **维度坍缩** | 一次合作繁荣-崩溃后，长期回不到合作者占多数 | 维度坍缩 | ⚠️ 容易出现，但**当前没有坍缩告警** |
| **搭便车** | 部分个体分享集群里蹭收益但极少分享给别人 | 伪善 / 搭便车主义 | ❌ 当前分享是对称的，没有"接收/给予不对称"机制 |
| **好善疾恶** | 群体主动处决/驱逐攻击者 | 善选择 / GVBE | ❌ 当前**没有 punish 动作**，玩家也无法手动处决 |
| **群体选择** | 整个群落被另一个群落取代 | 群体选择 / 群体竞争选择 | ❌ 当前**没有群落结构** |
| **内共生** | 两个个体融合成一个 | 内共生 | ❌ 当前**没有融合事件** |

**调种子的建议**：
- `seed 99` —— 干净的「夺利接管」轨迹（最适合看霍布斯丛林形成）
- `seed 42` —— 中段会有一次「合作繁荣-崩溃」脉冲（最适合看混沌对抗困境）
- 随便 `R` 看新种子，每个都不一样

---

## 六、调参入口（想自己改的）

集中在 `game/adversarial_evolution.py` 顶部：

- 食物 / 个体：`INIT_POP` / `INIT_FOOD` / `FOOD_TARGET` / `FOOD_SPAWN_PER_TICK`
- 个体能量学：`MAX_ENERGY` / `START_ENERGY` / `METAB_BASE` / `METAB_SCALE`
- 繁殖闸门：`REPRO_BASE` / `REPRO_SCALE` 与 `_agent_act` 末尾的 `0.05` 概率
- 攻击/分享阈值在 `_agent_act` 内 inline

每改一组参数，建议跑：

```bash
SDL_VIDEODRIVER=dummy DEBUG_TELEMETRY=1 python -u game/adversarial_evolution.py --ticks 1500 --seed 42
```

观察 `avgAgg` / `avgCoop` / `pop` 三个数随时间怎么走。

---

## 七、来源

书仓在工作区根 `repo/` 目录。所有游戏内术语译名以 `repo/核心词汇表.md` 为准。书中有一份作者自己写的离散策略模拟程序 `repo/对抗演化与合作跃升（下卷）——生命奇迹与永生哲学/03 附录/01 对抗演化模拟程序说明.md`，里面给出了 `修身 1.00 / 无条件利他 0.98 / 夺利 0.93 / TFT 0.80 / GVBE_1 0.60 ...` 的适应度参考表，是本游戏后续加「善选择 / punish」机制时的最佳数值校准起点。

---

## 八、八条核心动作的精确规则

下面是每个 tick 里**每个个体**按顺序执行的 8 个动作，写的是当前代码 `game/adversarial_evolution.py` 里 `_agent_act` 和 `World.step` 的真实行为。括号里的数是默认参数。

### 1️⃣ 代谢 (Metabolism) —— 每 tick 第一件事

```
self.energy -= 0.004 + 0.015 × self.metabolism
```

- `metabolism` ∈ [0, 1]，所以每 tick 扣 0.004 .. 0.019 能量
- **能量上限** `MAX_ENERGY = 2.0`，**下限** 0（≤ 0 即死，见 8️⃣）
- 平均 metab ≈ 0.5 → 每 tick 扣 ~0.012，满血 → 饿死约 **167 tick**

### 2️⃣ 感知 (Sense) —— 决定本 tick 谁能影响你

```
sense_radius = 18 + 80 × self.sense   # 像素
neighbors = 距自己 < sense_radius 的所有个体 (toroidal)
```

- `sense` ∈ [0, 1]，所以半径 18 .. 98 像素
- 屏幕宽度 960，半径 98 大概覆盖屏幕 ~10% 区域
- 低 sense ≈ 半瞎子 ≈ **天然修身**（无法攻击/分享也找不到食物）

### 3️⃣ 攻击 (夺利)

```
if self.energy < 0.45  AND  neighbors  AND  random() < self.aggression:
    target = neighbors 里 energy 最低的 (加 0~0.05 随机扰动打破平局)
    if target.energy < self.energy × 1.4:        # 不打比自己强 40% 以上的
        taken = 0.35 × (1 - 0.6 × target.defense)
        self.energy  += taken - 0.06              # 攻击者：得 stolen - 夺利税
        target.energy -= taken                    # 目标：直接扣 stolen
```

**能量公式（典型 agg=0.8, target.defense=0.3）**：
- `taken = 0.35 × (1 - 0.18) = 0.287`
- 攻击者净得：`+0.287 - 0.06 = +0.227`
- 目标净失：`-0.287`
- 系统净损耗：`0.06`（夺利税） —— 这就是 `1+1<2` 的精确实现

**触发不到的原因**：① 能量 ≥ 0.45（不饿就懒得打）② 周围没人 ③ aggression 太低 ④ 对方比自己强太多 ⑤ 这 tick 的 random 摇骰没命中

### 4️⃣ 分享 (合作)

```
if neighbors  AND  random() < self.cooperation × 0.6:
    same = [o for o in neighbors if hue距离(o) < 35°]
    if same:
        partner = same 里距离最近的
        if self.energy > 0.5  AND  partner.energy < 0.7:
            self.energy    -= 0.06
            partner.energy += 0.06            # 1:1 转移
```

**严格说这是 `1+1=2`，不是 `1+1>2`**。字面 `1+1>2` 在当前代码里是**间接涌现**：同色分享 → 自然聚簇 → 聚簇里多个分享流叠加 → 整体比独行者活得久 → 集群红利 ≈ 1+1>2。

**触发不到的原因**：① 周围没**同色**邻居（hue 距 ≥ 35°，隐形门槛）② cooperation 太低 ③ 自己能量 ≤ 0.5（不愿分）④ 对方能量 ≥ 0.7（已经够了不贪）

### 5️⃣ 移动

```
speed = 1 + 6 × self.speed            # 每 tick 移动 1..7 像素
if self.energy < 0.94:                # 没吃饱到能繁殖
    bias = 朝最近食物的单位向量
else:
    bias = (0, 0)                     # 吃饱就乱走
direction = 0.6 × 随机单位向量 + 0.4 × bias
self.x += direction.x × speed         # toroidal 环绕
self.y += direction.y × speed
```

- 阈值 `0.94 = REPRO_BASE + REPRO_SCALE × 0.4 = 0.85 + 0.5×0.4`
- 关键设计：**一旦能量过繁殖阈值的 80%，就不再偏向食物**，逼个体持续探索

### 6️⃣ 吃

```
for food in self.food:
    if 这 tick 还没吃过  AND  |food.x - self.x| < 8  AND  |food.y - self.y| < 8:
        self.energy = min(2.0, self.energy + food.amount)
        吃掉这团食物
        标记 ate = True
```

- 食物初始值 0.25 .. 0.55，**每 tick 每个体最多吃 1 团**
- 食物被吃后从 `self.food` 列表移除

### 7️⃣ 繁殖

```
repro_need = 0.85 + 0.5 × self.repro_thr    # 0.85 .. 1.35
if self.energy > repro_need  AND  random() < 0.05:
    child = clone(self) with traits += Gaussian(0, max(0.02, mut_rate × 0.25))
    self.energy /= 2                          # 一半能量给子代
```

- `repro_thr` ∈ [0, 1] 决定繁殖门槛
- 子代 8 个 trait 各自独立加一个高斯噪声，噪声标准差 = `max(0.02, mut_rate × 0.25)`
- 能量按 50/50 切（父 + 子总和不变）
- **5% 每 tick** 是关键低概率 —— 防止爆炸，但阈值一旦突破就会持续尝试

### 8️⃣ 死亡

```
if self.energy <= 0: die()
```

- **唯一死因**：能量耗尽（饿死 / 被抢死 / 被分享耗光）
- **没有自然衰老** —— `Agent.age` 字段记了但**完全不用**
- 死亡时 `world.deaths += 1`，agent 从 `self.agents` 移除

---

## 九、其他常被问到的规则

### 同色判定（决定谁能跟谁分享）

```python
def trait_hue(t):
    return degrees(atan2(t.aggression - 0.5, t.cooperation - 0.5)) % 360
```

- `hue 距 < 35°` 算同色
- 同色判定只在**分享**时用，不影响攻击（攻击谁都可以）

### 食物 spawn

```
target = max(20, FOOD_TARGET)          # 当前 FOOD_TARGET = 140
if len(food) < target:
    spawn FOOD_SPAWN_PER_TICK = 3 团，每团 amount = 0.25..0.55，位置全随机
```

- 食物总量是**绝对值**，不随人口扩张 → 人口膨胀时单位个体食物下降 → **密度依赖的生存压力**自然涌现

### 阶段分类（自动判定当前剧情阶段）

每 tick 调用 `World.current_stage()`，按以下优先级匹配第一个条件：

| 顺序 | 阶段 | 触发条件 |
|---|---|---|
| 1 | 起始混乱 | tick < 60 |
| 2 | 空 | 无活体 |
| 3 | 濒临灭绝 | pop < 25 |
| 4 | 霍布斯丛林 | avgAgg > 0.72 **且** avgCoop < 0.40 |
| 5 | 伊甸园期 | avgCoop > 0.55 **且** avgAgg < 0.50 **且** pop Δ < ±20 (近 60 tick) |
| 6 | 合作繁荣中 | avgCoop > 0.50 **且** avgAgg < 0.55 **且** pop Δ > +25 |
| 7 | 维度坍缩中 | pop Δ < -40 (近 60 tick) |
| 8 | 夺利丛林 | avgAgg > 0.62 |
| 9 | 朴素合作 | avgCoop > 0.48 **且** good_rate > 0.35 |
| 10 | 修身主导 | avgAgg < 0.40 **且** avgCoop < 0.40 |
| 11 | 未分化 | 其余 |

### 善者（good）的定义

```
good_rate = fraction(agents where cooperation > 0.5)
```

HUD 上「善者存活率」就是这个东西的时间曲线（最近 220 tick 的 sparkline）。

### 游戏里的「8 个 trait」具体行为

| trait | 取值 | 影响 |
|---|---|---|
| `aggression` | [0, 1] | 攻击摇骰命中率（饥饿时） |
| `cooperation` | [0, 1] | 分享摇骰命中率（实际阈值 = 此值 × 0.6） |
| `metabolism` | [0, 1] | 每 tick 能量扣除系数 |
| `speed` | [0, 1] | 每 tick 移动 1+6×speed 像素 |
| `sense` | [0, 1] | 感知半径 18+80×sense 像素 |
| `defense` | [0, 1] | 被攻击时减伤（最多减 60% 的 stolen） |
| `repro_thr` | [0, 1] | 繁殖能量门槛 0.85+0.5×repro_thr |
| `mut_rate` | [0, 1] | 子代 trait 噪声标准差 = max(0.02, mut_rate×0.25) |

8 个 trait 在初始化时**全部独立 uniform[0,1]** 随机；颜色（hue）由 `(aggression, cooperation)` 映射；策略标签完全靠 trait 范围**后验分类**，不在代码里硬编码。